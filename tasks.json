{
  "tasks": {
    "build": [
      "[format]",
      "tsc --project tsconfig.json"
    ],
    "format": [
      "jsonsort *.json",
      "prettier --loglevel silent --write \"src/**/*.ts\""
    ]
  }
}
